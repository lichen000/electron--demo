{
  "name": "electron-demo",
  "version": "1.0.0",
  "description": "A demo electron application",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . ElectronDemo --out=../ --platform=win32 --arch=x64 --electron-version=1.8.4"
  },
  "keywords": [
    "electron"
  ],
  "author": "mangolost",
  "license": "MIT",
  "devDependencies": {
    "electron": "^2.0.8",
    "electron-packager": "^11.1.0"
  }
}
